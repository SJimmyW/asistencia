# Google Sheets

Google Sheets is the project's database.
The application should work with a copy of the template spreadsheet.
Teachers are expected to modify the spreadsheet, never the code.
The application validates the spreadsheet structure automatically.
